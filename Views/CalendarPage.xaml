<Page x:Class="GardenApp.Views.CalendarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Календарь">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📅 Календарь посадок и сбора урожая" FontSize="28" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,20"/>

        <ListView x:Name="HarvestList" Grid.Row="1" Height="250">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Растение" Width="150" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Дата посадки" Width="120" DisplayMemberBinding="{Binding PlantingDate, StringFormat={}{0:dd.MM.yyyy}}"/>
                    <GridViewColumn Header="Статус урожая" Width="300">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="{Binding HarvestInfo}" Tag="{Binding}" Click="ShowHarvestInfo_Click" Background="#FF9800" Foreground="White" Margin="2"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <Border Grid.Row="2" Background="#FFF8E1" CornerRadius="10" Padding="15" Margin="0,20,0,0">
            <TextBlock x:Name="SelectedInfoText" Text="Нажмите на кнопку у растения, чтобы увидеть информацию об урожае" TextWrapping="Wrap"/>
        </Border>
    </Grid>
</Page>